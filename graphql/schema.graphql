type User {
  uid: Int
  email: String
  name: String
  isAdmin: Boolean
  createdAt: String
  updatedAt: String
}

type Query {
  users: [User]
}

type Mutation {
  addUser(userId: String!, userName: String!): Boolean
}